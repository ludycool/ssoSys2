<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace必须是接口的全类名 -->
<mapper namespace="jifan.ssosys.mapper.CommonBusinessMapper">
    <!-- 条件查 list item  v_userrole-->
    <select id="mylist" resultType="VUserrole" >
        select * from v_userrole
    </select>
</mapper>
